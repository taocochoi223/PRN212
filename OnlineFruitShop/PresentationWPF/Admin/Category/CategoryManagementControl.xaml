<UserControl x:Class="PresentationWPF.CategoryManagementControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#F9FAFB">
    <Grid Margin="20">
        <Border Background="White" CornerRadius="12" Padding="20">
            <DockPanel>
                <TextBlock Text="📂 Quản lý danh mục"
                           FontSize="24" FontWeight="Bold" Foreground="#333"
                           DockPanel.Dock="Top" Margin="0,0,0,20"/>

                <StackPanel Orientation="Horizontal" DockPanel.Dock="Top" Margin="0,0,0,15">
                    <Button Content="➕ Thêm danh mục"
                            Width="150" Height="35"
                            Margin="0,0,10,0"
                            Background="#28A745" Foreground="White"
                            Click="BtnAdd_Click"/>
                </StackPanel>

                <DataGrid x:Name="dgCategories"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          RowHeight="40"
                          AlternatingRowBackground="#F3F4F6">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding CategoryId}" Width="100"/>
                        <DataGridTextColumn Header="Tên danh mục" Binding="{Binding CategoryName}" Width="*"/>
                        <DataGridTextColumn Header="Mô tả" Binding="{Binding Description}" Width="2*"/>
                        <DataGridTemplateColumn Header="Hành động" Width="180">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <Button Content="✏ Sửa" Background="#007BFF" Foreground="White"
                                                Margin="4,0" Click="BtnEdit_Click"/>
                                        <Button Content="🗑 Xóa" Background="#DC3545" Foreground="White"
                                                Margin="4,0" Click="BtnDelete_Click"/>
                                    </StackPanel>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </DockPanel>
        </Border>
    </Grid>
</UserControl>
