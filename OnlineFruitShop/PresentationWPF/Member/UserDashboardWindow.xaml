<Window x:Class="PresentationWPF.Member.UserDashboardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PresentationWPF.Member"
        mc:Ignorable="d"
        Title="FruitShop - Trang người dùng" Height="600" Width="1000"
        WindowStartupLocation="CenterScreen" Background="#F9FBF9">

    <Window.Resources>
        <Style x:Key="MenuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter VerticalAlignment="Center" Margin="10,0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="200"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- SIDEBAR -->
        <StackPanel Background="#2C3E50">
            <TextBlock Text="🍏 Fruit User" FontSize="20" Foreground="White" FontWeight="Bold" Margin="10" />

            <Button Content="🏠 Trang chủ" Click="Home_Click" Style="{StaticResource MenuButtonStyle}" />
            <Button Content="🍎 Sản phẩm" Click="Products_Click" Style="{StaticResource MenuButtonStyle}" />
            <Button Content="🛒 Giỏ hàng" Click="Cart_Click" Style="{StaticResource MenuButtonStyle}" />
            <Button Content="📦 Đơn hàng" Click="Orders_Click" Style="{StaticResource MenuButtonStyle}" />
            <Button Content="👤 Tài khoản" Click="Account_Click" Style="{StaticResource MenuButtonStyle}" />
            <Button Content="🚪 Đăng xuất" Click="Logout_Click" Style="{StaticResource MenuButtonStyle}"/>
        </StackPanel>

        <!-- MAIN CONTENT -->
        <ContentControl x:Name="MainContent" Grid.Column="1" Margin="10"/>
    </Grid>
</Window>
