<UserControl x:Class="PresentationWPF.Member.CartControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PresentationWPF.Member"
             Background="{StaticResource BackgroundColor}">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="../Styles/CommonStyles.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel Margin="40">
            <!-- Modern Page Header -->
            <StackPanel Margin="0,0,0,40">
                <TextBlock Text="🛒 Giỏ hàng của bạn" Style="{StaticResource PageHeaderStyle}"/>
                <TextBlock Text="Quản lý các sản phẩm bạn muốn mua"
                           FontSize="16"
                           Foreground="{StaticResource TextSecondaryColor}"
                           HorizontalAlignment="Center"/>
            </StackPanel>

            <!-- Modern Cart Content -->
            <Border Style="{StaticResource ModernCardStyle}" Margin="0,0,0,30">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <TextBlock Text="📋 Danh sách sản phẩm" Style="{StaticResource SectionTitleStyle}"/>
                        <Border Background="{StaticResource InfoColor}"
                                CornerRadius="12"
                                Padding="8,4"
                                Margin="15,0,0,0"
                                VerticalAlignment="Center">
                            <TextBlock x:Name="txtItemCount"
                                       Text="0 sản phẩm"
                                       Foreground="White"
                                       FontSize="12"
                                       FontWeight="SemiBold"/>
                        </Border>
                    </StackPanel>

                    <!-- Modern Cart Items DataGrid -->
                    <DataGrid x:Name="dgCart"
                              Style="{StaticResource ModernDataGridStyle}"
                              ColumnHeaderStyle="{StaticResource ModernDataGridHeaderStyle}"
                              CurrentCellChanged="dgCart_CurrentCellChanged"
                              MinHeight="350">
                        <DataGrid.Columns>
                            <!-- Select Column -->
                            <DataGridCheckBoxColumn Header="✓ Chọn" Binding="{Binding IsSelected, Mode=TwoWay}" Width="80">
                                <DataGridCheckBoxColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridHeaderStyle}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    </Style>
                                </DataGridCheckBoxColumn.HeaderStyle>
                            </DataGridCheckBoxColumn>

                            <!-- Product Name Column -->
                            <DataGridTextColumn Header="🍎 Sản phẩm" Binding="{Binding ProductName}" Width="*">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryColor}"/>
                                        <Setter Property="Padding" Value="10,5"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                            </DataGridTextColumn>

                            <!-- Quantity Column -->
                            <DataGridTextColumn Header="📦 Số lượng" Binding="{Binding Quantity}" Width="120">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="HorizontalAlignment" Value="Center"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="FontSize" Value="14"/>
                                        <Setter Property="Foreground" Value="{StaticResource InfoColor}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridHeaderStyle}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <!-- Unit Price Column -->
                            <DataGridTextColumn Header="💰 Đơn giá" Width="140">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding UnitPrice, StringFormat={}{0:N0}₫}"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="FontWeight" Value="SemiBold"/>
                                        <Setter Property="Foreground" Value="{StaticResource TextSecondaryColor}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridHeaderStyle}">
                                        <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <!-- Total Price Column -->
                            <DataGridTextColumn Header="💵 Thành tiền" Width="160">
                                <DataGridTextColumn.ElementStyle>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="{Binding Total, StringFormat={}{0:N0}₫}"/>
                                        <Setter Property="HorizontalAlignment" Value="Right"/>
                                        <Setter Property="FontWeight" Value="Bold"/>
                                        <Setter Property="FontSize" Value="15"/>
                                        <Setter Property="Foreground" Value="{StaticResource PrimaryColor}"/>
                                    </Style>
                                </DataGridTextColumn.ElementStyle>
                                <DataGridTextColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridHeaderStyle}">
                                        <Setter Property="HorizontalContentAlignment" Value="Right"/>
                                    </Style>
                                </DataGridTextColumn.HeaderStyle>
                            </DataGridTextColumn>

                            <!-- Actions Column -->
                            <DataGridTemplateColumn Header="⚙️ Thao tác" Width="220">
                                <DataGridTemplateColumn.HeaderStyle>
                                    <Style TargetType="DataGridColumnHeader" BasedOn="{StaticResource ModernDataGridHeaderStyle}">
                                        <Setter Property="HorizontalContentAlignment" Value="Center"/>
                                    </Style>
                                </DataGridTemplateColumn.HeaderStyle>
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
                                            <!-- Decrease Button -->
                                            <Button Content="➖"
                                                    Width="32" Height="32"
                                                    Background="{StaticResource DangerColor}"
                                                    Foreground="White"
                                                    BorderThickness="0"
                                                    FontWeight="Bold"
                                                    FontSize="12"
                                                    Click="DecreaseQuantity_Click"
                                                    ToolTip="Giảm số lượng"
                                                    Margin="0,0,8,0"
                                                    Cursor="Hand">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="6">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{StaticResource PrimaryDarkColor}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>

                                            <!-- Increase Button -->
                                            <Button Content="➕"
                                                    Width="32" Height="32"
                                                    Background="{StaticResource SuccessColor}"
                                                    Foreground="White"
                                                    BorderThickness="0"
                                                    FontWeight="Bold"
                                                    FontSize="12"
                                                    Click="IncreaseQuantity_Click"
                                                    ToolTip="Tăng số lượng"
                                                    Margin="0,0,8,0"
                                                    Cursor="Hand">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="6">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="#229954"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>

                                            <!-- Delete Button -->
                                            <Button Content="🗑️"
                                                    Width="32" Height="32"
                                                    Click="Delete_Click"
                                                    Background="{StaticResource MediumGrayColor}"
                                                    Foreground="White"
                                                    BorderThickness="0"
                                                    FontSize="12"
                                                    ToolTip="Xóa khỏi giỏ hàng"
                                                    Cursor="Hand">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}"
                                                                CornerRadius="6">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                                        </Border>
                                                        <ControlTemplate.Triggers>
                                                            <Trigger Property="IsMouseOver" Value="True">
                                                                <Setter Property="Background" Value="{StaticResource DangerColor}"/>
                                                            </Trigger>
                                                        </ControlTemplate.Triggers>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                        </StackPanel>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </StackPanel>
            </Border>

            <!-- Modern Total and Checkout Section -->
            <Border Style="{StaticResource ModernCardStyle}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Total Amount Section -->
                    <StackPanel Grid.Column="0" VerticalAlignment="Center">
                        <TextBlock Text="💰 Tổng tiền cần thanh toán"
                                   FontSize="18"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource TextPrimaryColor}"
                                   Margin="0,0,0,8"/>
                        <TextBlock x:Name="txtTotal"
                                   Text="0₫"
                                   FontSize="28"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource PrimaryColor}"/>
                        <TextBlock Text="(Đã bao gồm VAT)"
                                   FontSize="12"
                                   Foreground="{StaticResource TextSecondaryColor}"
                                   Margin="0,5,0,0"/>
                    </StackPanel>

                    <!-- Action Buttons -->
                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <Button Content="🛍️ Đặt hàng ngay"
                                Style="{StaticResource PrimaryButtonStyle}"
                                FontSize="16"
                                Padding="30,15"
                                Margin="0,0,0,10"
                                Click="PlaceOrder_Click"/>
                        <Button Content="🗑️ Xóa tất cả"
                                Style="{StaticResource SecondaryButtonStyle}"
                                FontSize="14"
                                Padding="30,12"
                                Click="ClearAll_Click"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- Cart Actions -->
            <Border Style="{StaticResource ModernCardStyle}" Margin="0,20,0,0">
                <StackPanel>
                    <TextBlock Text="🎯 Thao tác nhanh" Style="{StaticResource SectionHeaderStyle}"/>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                        <Button Content="✅ Chọn tất cả"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0,0,10,0"
                                Click="SelectAll_Click"/>
                        <Button Content="❌ Bỏ chọn tất cả"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Margin="0,0,10,0"
                                Click="UnselectAll_Click"/>
                        <Button Content="🗑️ Xóa đã chọn"
                                Style="{StaticResource DangerButtonStyle}"
                                Click="DeleteSelected_Click"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
