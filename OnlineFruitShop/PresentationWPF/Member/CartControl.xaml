<UserControl x:Class="PresentationWPF.Member.CartControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:PresentationWPF.Member"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Danh sách giỏ hàng -->
        <DataGrid x:Name="dgCart"
          AutoGenerateColumns="False"
          CanUserAddRows="False"
          IsReadOnly="False"
          SelectionMode="Extended"
          Margin="0,0,0,10"
          CurrentCellChanged="dgCart_CurrentCellChanged">
            <DataGrid.Columns>
                <DataGridCheckBoxColumn Header="Chọn" Binding="{Binding IsSelected, Mode=TwoWay}" Width="60"/>
                <DataGridTextColumn Header="Sản phẩm" Binding="{Binding ProductName}" Width="*"/>
                <DataGridTextColumn Header="Số lượng" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Đơn giá (₫)" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="{Binding UnitPrice, StringFormat={}{0:N0}}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Thành tiền (₫)" Width="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="Text" Value="{Binding Total, StringFormat={}{0:N0}}"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTemplateColumn Header="Xóa" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="❌" Click="Delete_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Tổng tiền và nút Đặt hàng -->
        <Grid Grid.Row="1" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" Grid.Column="1" HorizontalAlignment="Right">
                <TextBlock Text="Tổng tiền: " FontWeight="Bold" VerticalAlignment="Center"/>
                <TextBlock x:Name="txtTotal" Foreground="Green" FontSize="16" FontWeight="Bold" Margin="5,0"/>
                <Button Content="Đặt hàng" Click="PlaceOrder_Click" Background="Green" Foreground="White" Padding="10,5" Margin="20,0,0,0"/>

            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
