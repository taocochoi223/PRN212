<Window x:Class="PresentationWPF.Member.SimpleOrderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Thông tin đặt hàng" Height="400" Width="500" 
        WindowStartupLocation="CenterOwner"
        Background="#F8F9FA">

    <Border Margin="20" Background="White" CornerRadius="8" Padding="20">
        <StackPanel>
            <TextBlock Text="📋 Thông tin giao hàng" 
                       FontSize="18" 
                       FontWeight="Bold" 
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Receiver Name -->
                <TextBlock Grid.Row="0" Grid.Column="0" 
                           Text="Tên người nhận:" 
                           FontWeight="SemiBold" 
                           VerticalAlignment="Center"
                           Margin="0,0,10,15"/>
                <TextBox Grid.Row="0" Grid.Column="1" 
                         x:Name="txtReceiverName" 
                         Height="30"
                         Margin="0,0,0,15"/>

                <!-- Phone -->
                <TextBlock Grid.Row="1" Grid.Column="0" 
                           Text="Số điện thoại:" 
                           FontWeight="SemiBold" 
                           VerticalAlignment="Center"
                           Margin="0,0,10,15"/>
                <TextBox Grid.Row="1" Grid.Column="1" 
                         x:Name="txtPhone" 
                         Height="30"
                         Margin="0,0,0,15"/>

                <!-- Address -->
                <TextBlock Grid.Row="2" Grid.Column="0" 
                           Text="Địa chỉ giao hàng:" 
                           FontWeight="SemiBold" 
                           VerticalAlignment="Top"
                           Margin="0,10,10,20"/>
                <TextBox Grid.Row="2" Grid.Column="1" 
                         x:Name="txtAddress" 
                         Height="60"
                         TextWrapping="Wrap"
                         AcceptsReturn="True"
                         VerticalScrollBarVisibility="Auto"
                         Margin="0,0,0,20"/>

                <!-- Buttons -->
                <StackPanel Grid.Row="3" Grid.Column="1" 
                            Orientation="Horizontal" 
                            HorizontalAlignment="Right" 
                            Margin="0,10,0,0">
                    <Button Content="Hủy" 
                            Width="80" 
                            Height="35"
                            Click="Cancel_Click" 
                            Margin="0,0,15,0"/>
                    <Button Content="Xác nhận" 
                            Width="100" 
                            Height="35"
                            Background="#27ae60"
                            Foreground="White"
                            Click="Confirm_Click"/>
                </StackPanel>
            </Grid>
        </StackPanel>
    </Border>
</Window>
